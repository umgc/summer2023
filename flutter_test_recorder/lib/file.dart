      // try {
      //   final filePath = '${dir.path}/filename.txt';
      //   final file = File(filePath);
      //   await file.writeAsString('hello');
      //   final reloadedFile = File(filePath);
      //   print(
      //       'Wrote file: filePath: $filePath, length: ${await reloadedFile.length()}');
      //   // _dialogBuilder('File Written',
      //   //     'filePath: $filePath, length: ${await reloadedFile.length()}');
      // } catch (error) {
      //   _errorDialogBuilder(context, error.toString());
      // }
